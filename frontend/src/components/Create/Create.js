import React from 'react';
import styled from 'styled-components';
const Create = () => {
  const CreateContainer = styled.div`
    display: flex;
    justify-content: center;
  `;
  return <CreateContainer>글작성</CreateContainer>;
};

export default Create;
